# --- 数据库配置 ---
# Cloud MongoDB Atlas 连接串
# 获取方式: MongoDB Atlas -> Database -> Connect -> Drivers -> Node.js
# 格式: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
NUXT_MONGODB_URI=mongodb+srv://user:password@cluster.example.mongodb.net/megumi_lite?retryWrites=true&w=majority

# --- Redis 配置 (Upstash 云端) ---
# 获取方式: Upstash -> Database -> Connect -> Node.js (ioredis)
NUXT_REDIS_HOST=your-instance.upstash.io
NUXT_REDIS_PORT=6379
# Upstash 的长密码 (TLS 自动开启)
NUXT_REDIS_PASSWORD=your_upstash_password_here

# --- 管理员配置 ---
NUXT_ADMIN_USERNAME=admin
# ⚠️ 生产环境请务必修改此密码
NUXT_ADMIN_PASSWORD=change_this_to_strong_password

# --- 安全配置 ---
# 用于加密 Cookie 的密钥，请生成一个随机的长字符串
# 可以用 openssl rand -hex 32 生成
NUXT_AUTH_SECRET=random_secret_string_at_least_32_chars

# --- 公开配置 ---
# 本地开发填 http://localhost:3000，线上部署填 Vercel 分配的域名
NUXT_PUBLIC_SITE_URL=http://localhost:3000
# 统计配置 (默认 umami, 可选 51la)
NUXT_PUBLIC_ANALYTICS_PROVIDER=umami
# 你的 Umami 分享链接 (如果没有先留空)
NUXT_PUBLIC_ANALYTICS_SHARE_URL=